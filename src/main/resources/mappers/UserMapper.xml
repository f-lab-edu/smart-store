<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.smartStore.mapper.UserMapper">
    <insert id="insertUser" parameterType="com.project.smartStore.dto.UserDto">
        INSERT INTO users(id, password, email, name, phone)
        VALUES(#{id}, #{password}, #{email}, #{name}, #{phone})
    </insert>

    <select id="isUsingId" parameterType="String" resultType="boolean">
        SELECT EXISTS
        (SELECT id FROM users WHERE id = #{id})
    </select>
</mapper>